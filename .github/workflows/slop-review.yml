name: Slop Review

on:
  workflow_call:
    inputs:
      runs-on:
        description: JSON array of runner labels to target.
        type: string
        default: '["self-hosted", "macOS", "ARM64", "ci-scope"]'
      config:
        description: Path to the slop-review config in the calling repo.
        type: string
        default: .slop-review.json
      gates-ref:
        description: ci-gates ref to fetch scripts from.
        type: string
        default: main
      model:
        description: Ollama model used for review; empty string disables the job.
        type: string
        default: qwen3-coder:30b-a3b-q4_K_M

jobs:
  slop-review:
    name: Slop Review
    runs-on: ${{ fromJSON(inputs['runs-on']) }}

    steps:
      - name: Checkout project
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Fetch ci-gates
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$RUNNER_TEMP/ci-gates"
          git clone --quiet --depth 1 --branch "${{ inputs.gates-ref }}" \
            https://github.com/ForkHorizon/ci-gates "$RUNNER_TEMP/ci-gates"

      - name: Resolve review range
        id: range
        shell: bash
        run: |
          set -euo pipefail
          case "${{ github.event_name }}" in
            pull_request)
              echo "base=${{ github.event.pull_request.base.sha }}" >> "$GITHUB_OUTPUT"
              echo "head=${{ github.sha }}" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "base=origin/${{ github.event.repository.default_branch }}" >> "$GITHUB_OUTPUT"
              echo "head=HEAD" >> "$GITHUB_OUTPUT"
              ;;
          esac

      - name: Review diff for AI slop
        if: ${{ inputs.model != '' }}
        shell: bash
        run: |
          set -euo pipefail
          python3 "$RUNNER_TEMP/ci-gates/scripts/slop-review.py" \
            --base "${{ steps.range.outputs.base }}" \
            --head "${{ steps.range.outputs.head }}" \
            --config "${{ inputs.config }}" \
            --model "${{ inputs.model }}"
